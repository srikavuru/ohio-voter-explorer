Read CLAUDE.md in full before doing anything. Then scaffold Phase 0: initialize a React + Vite + Tailwind project in this directory, create the /api folder with its own package.json for Azure Functions (Node.js runtime) including firebase-admin and @anthropic-ai/sdk dependencies, add staticwebapp.config.json with navigation fallback, create .gitignore covering api/local.settings.json, .env, node_modules, and data/ (the raw voter file CSV never gets committed), create api/local.settings.json with placeholder values for FIREBASE_SERVICE_ACCOUNT and ANTHROPIC_API_KEY (no real secrets), create src/lib/firebase.js and src/lib/api.js with the correct structure per CLAUDE.md, create src/App.jsx with a basic auth gate and router shell (pages: Dashboard, VoterSearch, VoterDetail, AskAI), and create api/shared/auth.js with the Firebase token verification helper that returns dev-user-local when no auth header is present. Do NOT build the CSV import or data-store logic yet — that's Phase 1 and depends on the row count of the actual downloaded file, which we haven't checked yet. Do not install packages yet — just create the files. Show me the structure when done.
